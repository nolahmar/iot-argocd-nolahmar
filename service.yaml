apiVersion: v1
kind: Service
metadata:
  name: nolahmar-playground-service
  namespace: dev
spec:
  selector:
    app: nolahmar-playground
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
  type: LoadBalancer
